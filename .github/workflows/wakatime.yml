name: Wakatime

on:
  workflow_dispatch:
  schedule:
    - cron: '30 15 * * *' # 12:30 am (JST)

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@0.1.3
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
